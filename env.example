# Supabase Configuration
# Get these from your Supabase project settings -> API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# WhatsApp Business API Configuration (for Edge Functions)
# These are set in Supabase Dashboard -> Edge Functions -> Secrets
# WHATSAPP_API_URL=https://graph.facebook.com/v17.0
# WHATSAPP_API_TOKEN=your-whatsapp-api-token
# WHATSAPP_PHONE_NUMBER_ID=your-phone-number-id

# JWT Secret for custom token generation
# Set in Supabase Dashboard -> Edge Functions -> Secrets
# JWT_SECRET=your-secure-jwt-secret

# ============================================
# SETUP INSTRUCTIONS
# ============================================
#
# 1. Create a Supabase project at https://supabase.com
#
# 2. Copy this file to .env.local and fill in your values:
#    cp env.example .env.local
#
# 3. Run the schema.sql file in Supabase SQL Editor:
#    - Go to SQL Editor in your Supabase dashboard
#    - Copy contents of supabase/schema.sql
#    - Run the query
#
# 4. Create the storage bucket:
#    - Go to Storage in Supabase dashboard
#    - Create a new bucket called "creator-photos"
#    - Set it to private (not public)
#
# 5. Deploy Edge Functions:
#    supabase functions deploy send-whatsapp-otp
#    supabase functions deploy verify-whatsapp-otp
#
# 6. Set Edge Function secrets:
#    supabase secrets set WHATSAPP_API_URL=https://graph.facebook.com/v17.0
#    supabase secrets set WHATSAPP_API_TOKEN=your-token
#    supabase secrets set WHATSAPP_PHONE_NUMBER_ID=your-phone-id
#    supabase secrets set JWT_SECRET=your-secret
#
# 7. For WhatsApp OTP template, create a template in Meta Business Suite:
#    - Template name: otp_verification
#    - Category: AUTHENTICATION
#    - Body: Your Hush verification code is {{1}}
#
# ============================================
# DEVELOPMENT MODE
# ============================================
#
# If you don't set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY,
# the app will run in mock mode with localStorage-only storage.
# This is useful for development without a backend.
